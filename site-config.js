/*
 * Yayına geçişte yalnızca aşağıdaki iki değeri güncelleyin.
 * siteUrl: Tercih edilen HTTPS alan adı (ör. https://www.example.com)
 * ga4MeasurementId: Google Analytics 4 web akışı kimliği (G- ile başlar)
 */
window.LIORA_CONFIG = Object.freeze({
  siteUrl: 'https://www.lioraortaca.com',
  ga4MeasurementId: '',
  inventory: Object.freeze({
    total: 8,
    available: 4
  })
});
